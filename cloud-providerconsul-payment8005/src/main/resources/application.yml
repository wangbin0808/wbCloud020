server:
  port: 8005


spring:
  application:
    name: cloud-payment-service # 服务名称
  cloud:
    consul:
      host: localhost
      port: 8500
      discovery:
        service-name: ${spring.application.name}


